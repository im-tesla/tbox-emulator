# OEM firmware/RAM dumps — used only for local reverse-engineering / verification.
# Never commit these (see the project disclaimer: no OEM firmware or binary blobs).
firmware.bin
ram.bin
*.bin

# OEM-derived reverse-engineering notes — keep local, do not publish.
REVERSE_ENGINEERING.md
ble.md

# macOS
.DS_Store
